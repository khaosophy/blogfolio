---
import { parse } from 'node-html-parser';
import dayjs from 'dayjs';
import PostLayout from '../../layouts/PostLayout.astro';

export async function getStaticPaths() {
  const response = await fetch('https://wp.khaosophy.com/wp-json/wp/v2/posts?per_page=100');
  const WpPosts = await response.json();
	const MdPosts = await Astro.glob('../../content/*.md');

	const allPosts = [
		MdPosts.map(post => {
			return {
					params: { post: post.frontmatter.slug },
					props: { post },
				}
		}),

		WpPosts.map(post => {
			return {
				params: { post: post.slug },
				props: {
					post: {
						title: post.title.rendered,
						content: parse(post.content.rendered),
						date: post.date,
						excerpt: post.excerpt.rendered,
					}
				},
			}
		})
	];

  return allPosts;
}
const { title, Content, content, date, excerpt } = Astro.props.post;
---
<PostLayout
	title={title}
	description={excerpt}
>
	<!-- todo: back should link to the post's page -->
	<small style="position: absolute;"><a href="/blog">← Back to Blog</a></small>
	<h1 style="
		margin-bottom: 0.4375rem;
		font-family: 'Montserrat', sans-serif;
	">{title}</h1>
	<small>Published {dayjs(date).format('MMMM D, YYYY')}</small>
	<div style="margin-top: 20px">
		<!-- if lowercase content is undefined (which comes from WP), render Astro's Markdown Content -->
		{content ? content : <Content />}
	</div>
</PostLayout>